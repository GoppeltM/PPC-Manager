<Page x:Class="Begegnungen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:PPC_Manager"
      xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      mc:Ignorable="d" Title="Begegnungen" d:DesignHeight="474" d:DesignWidth="900">
    <Page.Resources>
        <local:SatzFarbenPainter x:Key="MyPainter" />
        <local:StringFormatter x:Key="StringFormatter" />
        <local:AusgeschiedenPainter x:Key="AusgeschiedenPainter"/>
        <CollectionViewSource x:Key="PartieView" Filter="BegegnungenListView_Filter"
                              d:DesignSource="{Binding Source={d:DesignInstance Type=local:DesignSpielRunden, IsDesignTimeCreatable=True}, Path=/}"
                              Source="{Binding Source={StaticResource SpielRunden}, Path=/}" />        
        <CollectionViewSource x:Key="SpielerView"
                              d:DesignSource="{d:DesignInstance Type=local:DesignSpielerListe, IsDesignTimeCreatable=True}"
                      Source="{Binding Source={StaticResource SpielerListe}}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="Punkte" Direction="Descending"/>
                <scm:SortDescription PropertyName="BuchholzPunkte" Direction="Descending"/>
                <scm:SortDescription PropertyName="SatzDiff" Direction="Descending"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Page.Resources>
    <Page.CommandBindings>
        <CommandBinding Command="Favorites" Executed="BegegnungenFiltern_Executed"/>
        <CommandBinding Command="NextPage" Executed="NächsteRunde_Executed"/>
    </Page.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="96" />
            <RowDefinition Height="378*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>        
            <ListView Name="ListView1" SelectedIndex="-1" Margin="10,5,20,5" Grid.RowSpan="2" SelectionMode="Single"
                      ItemsSource="{Binding Source={StaticResource SpielerView}}"
                      Grid.Column="0">
            <ListView.CommandBindings>
                <CommandBinding Command="Delete" CanExecute="Ausscheiden_CanExecute" Executed="Ausscheiden_Executed"/>
            </ListView.CommandBindings>
                <ListView.ContextMenu>
                <ContextMenu>
                    <ContextMenu.Items>
                        <MenuItem Header="Spieler ausscheiden" Name="MenuItem1" Command="Delete" />
                    </ContextMenu.Items>
                </ContextMenu> 
            </ListView.ContextMenu>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ListViewItem Background="{Binding Path=Ausgeschieden, Converter={StaticResource AusgeschiedenPainter}}" IsHitTestVisible="False">                        
                        <Grid Margin="5" Width="180">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition/>
                                <ColumnDefinition Width="30" />
                                <ColumnDefinition Width="20" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>                                
                            </Grid.RowDefinitions>
                            <Grid.Resources>
                                <local:IndexConverter x:Key="Converter"/>
                            </Grid.Resources>
                            <Label Grid.Column="0" Grid.RowSpan="2" VerticalContentAlignment="Center" 
                                   FontSize="16" FontWeight="Bold" Foreground="#FF8D7979" HorizontalContentAlignment="Center"
                                   Content="1"
                                   />
                            <Label Grid.Column="1" Grid.Row="0" Content="{Binding Path=Vorname}" HorizontalContentAlignment="Center" />
                            <Label Grid.Column="1" Grid.Row="1" Content="{Binding Path=Nachname}" HorizontalContentAlignment="Center" />
                            <Label Grid.Column="2" Grid.Row="0" Content="{Binding Path=Punkte}" HorizontalContentAlignment="Center" ToolTip="Punkte" />
                            <Label Grid.Column="2" Grid.Row="1" Content="{Binding Path=BuchholzPunkte}" HorizontalContentAlignment="Center" ToolTip="Buchholzpunkte" />
                            <Label Grid.Column="3" Grid.Row="0" Content="{Binding Path=SätzeGewonnen}" HorizontalContentAlignment="Center" ToolTip="Gewonnene Sätze" />
                            <Line Grid.Column="3" Grid.RowSpan="2" Stroke="Black" StrokeThickness="2" Stretch="Fill" X2="1" Margin="2" ToolTip="Verlorene Sätze" />
                            <Label Grid.Column="3" Grid.Row="1" Content="{Binding Path=SätzeVerloren}" HorizontalContentAlignment="Center" />
                        </Grid>
                    </ListViewItem> 
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <GridSplitter Grid.Column ="0" Background="Blue" Width="5"
                HorizontalAlignment="Right" VerticalAlignment="Stretch" Grid.RowSpan="2" Margin="5" />
        <Label Content="{Binding Source={StaticResource SpielRunden}, Path=Count, Converter={StaticResource StringFormatter}, ConverterParameter=Runde: \{0\}}" Grid.Column="1" Height="54" HorizontalAlignment="Right" Margin="0,10,12,0" Name="RundenAnzeige" VerticalAlignment="Top" Width="141" FontSize="24" HorizontalContentAlignment="Right" />
        <ListBox Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" Name="SpielPartienView" VerticalAlignment="Stretch" FontSize="24" 
                 ItemsSource="{Binding Source={StaticResource PartieView}}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ActualWidth}" />
                </ItemsPanelTemplate>                
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.Resources>
                            <local:MeineGewonnenenSätze x:Key="MeineGewonnenenSätze" />
                        </Grid.Resources>
                        <Label Grid.Row="0" Grid.Column="0" Content="{Binding Path=SpielerLinks.StartNummer}"/>
                        <Label Grid.Row="1" Grid.Column="0" Content="{Binding Path=SpielerRechts.StartNummer}"/>
                        <Label Grid.Row="0" Grid.Column="1" Content="{Binding Path=SpielerLinks.Nachname}"/>
                        <Label Grid.Row="1" Grid.Column="1" Content="{Binding Path=SpielerRechts.Nachname}"/>
                        <ItemsControl Grid.Row="0" Grid.Column="2" ItemsSource="{Binding}">                            
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Ellipse Grid.Row="0" Grid.Column="2" Width="10" Height="10" Margin="2" Visibility="{Binding Path=PunkteLinks, Mode=TwoWay, Converter={StaticResource MeineGewonnenenSätze}}">
                                        <Ellipse.Fill>
                                            <RadialGradientBrush>
                                                <GradientStop Color="SpringGreen" Offset="0" />
                                                <GradientStop Color="#FF23654C" Offset="1" />
                                            </RadialGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl Grid.Row="1" Grid.Column="2" ItemsSource="{Binding}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Ellipse Grid.Row="0" Grid.Column="2" Width="10" Height="10" Margin="2" Visibility="{Binding Path=PunkteRechts, Mode=TwoWay, Converter={StaticResource MeineGewonnenenSätze}}">
                                        <Ellipse.Fill>
                                            <RadialGradientBrush>
                                                <GradientStop Color="SpringGreen" Offset="0" />
                                                <GradientStop Color="#FF23654C" Offset="1" />
                                            </RadialGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                    
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="0" Grid.Column="1"  HorizontalAlignment="Left" Margin="5" 
              Name="DetailGrid" VerticalAlignment="Stretch" DataContext="{Binding Path=/, Source={StaticResource PartieView}, Mode=TwoWay}" 
              d:DataContext="{x:Static local:DesignSpielPartien.DesignSpielPartie}"
              MinWidth="500">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.Resources>
                
                <Style x:Key="SatzListe" TargetType="ItemsControl">
                    <Setter Property="Grid.Column" Value="1"/>
                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                    <Setter Property="ItemsSource" Value="{Binding}" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Stretch" />                    
                    <Setter Property="ItemsPanel">
                        <Setter.Value>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>                            
                        </Setter.Value>
                    </Setter>
                </Style>             
            </Grid.Resources>
            <Label Grid.Column="0" Grid.Row="0" Content="{Binding Path=SpielerLinks.Nachname}"/>
            <Label Grid.Column="0" Grid.Row="1" Content="{Binding Path=SpielerRechts.Nachname}"/>
            <ItemsControl Grid.Row="0" Style="{StaticResource SatzListe}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Name="Button1" Click="SatzLinks_Click" DataContext="{Binding}" Background="{Binding Path=PunkteLinks, Converter={StaticResource MyPainter}}" MinWidth="30" Margin="5" />                        
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <ItemsControl Grid.Row="1" Style="{StaticResource SatzListe}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Name="Button1" Click="SatzRechts_Click" DataContext="{Binding}" 
                                Background="{Binding Path=PunkteRechts, Converter={StaticResource MyPainter}}" 
                                MinWidth="30" Margin="5" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</Page>
